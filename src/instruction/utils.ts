/**
 * Gets the binary value of a boolean
 * @param value boolean value of the bit
 * @returns string of the binary value
 */
export function getBinaryValue(value: boolean): string {
  return value ? '1' : '0'
}
