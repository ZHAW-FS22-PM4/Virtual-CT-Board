import { Byte } from 'types/binary'
import {add} from "../../../src/dummy/dummy";

describe("test add function", () => {
    test("should create new Bye", ()=> {
        //expect(Byte.fromUnsignedInteger(265)).toThrow(TypeError);
        //todo: improve this tests.
    });
});